<!DOCTYPE HTML>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, user-scalable=no, shrink-to-fit=no">

    <meta name="description" content="Primary Order Collection">
    <meta name="keywords" content="Primary Order, Primary, Order">
    <meta name=”theme-color” content=”#0d6efd>
    <meta name="apple-mobile-web-app-capable" content="yes">

    <title>@ViewBag.Title || PriOrder</title>

    <link href="~/Assets/images/favicon.ico" rel="shortcut icon" type="image/x-icon">

    <!-- Bootstrap -->
    <link href="~/Assets/css/bootstrap.css" rel="stylesheet" type="text/css" />

    <!-- custom style -->
    <link href="~/Assets/css/ui.css" rel="stylesheet" type="text/css" />

    <!-- Fonticon -->
    <link href="~/Assets/fonts/material-icon/css/round.css" rel="stylesheet" type="text/css" />

</head>
<body>

    <!-- Preloader-->
    <div class="preloader" id="preloader">
        <div class="spinner-grow" role="status">
            <span class="visually-hidden">Loading...</span>
        </div>
    </div>
    <!-- Preloader //end -->


    <header class="app-header ondark fixed-top bg-primary">

        <a href="#offcanvas_left_123" data-bs-toggle="offcanvas" role="button" class="btn-header">
            <i class="material-icons md-menu"></i>
        </a>

        <h5 class="title-header">@ViewBag.Title</h5>

        <a href="@Url.Action("Logout","Home")" class="btn-header">
            <i class="material-icons md-log_out"></i>
        </a>

    </header> <!-- app-header.// -->

    <main class="app-content">
        <section class="p-3">
            @RenderBody()
        </section>
        <br>
    </main> <!-- app-content.// -->

    <nav class="nav-bottom">

        @if (Session["menuBottom"] != null)
        {
            var bottomMenu = (List<PriOrder.App.Models.WO_APP_MENU>)Session["menuBottom"];
            foreach (var _menu in bottomMenu)
            {
                <a href="@Url.Action(_menu.ACTION_NAME,_menu.CONTROLLER_NAME)" class="nav-link">
                    <i class="icon material-icons @_menu.MENU_ICON"></i><span class="text">@_menu.MENU_NAME_EN</span>
                </a>
            }
        }




  
        @*<a href="@Url.Action("Categories","Products")" class="nav-link">
            <i class="icon material-icons md-apps"></i><span class="text">Category</span>
        </a>
        <a href="@Url.Action("Cart","Orders")" class="nav-link">
            <span class="float-end badge bg-warning">1</span>
            <i class="icon material-icons md-shopping_cart"></i><span class="text">Cart</span>
        </a>
        <a href="@Url.Action("Favorite","Products")" class="nav-link">
            <i class="icon material-icons md-favorite"></i><span class="text">Favorite</span>
        </a>
        <a href="@Url.Action("Messages")" class="nav-link">
            <span class="float-end badge bg-warning">2</span>
            <i class="icon material-icons md-chat"></i><span class="text">Messages</span>
        </a>*@
    </nav> <!-- nav-bottom -->


    <aside class="offcanvas offcanvas-start" tabindex="-1" id="offcanvas_left_123">
        <header class="p-3 border-bottom bg-light">
            <button type="button" class="float-end btn-close text-reset" data-bs-dismiss="offcanvas" aria-label="Close"></button>
            <a href="@Url.Action("MyProfile","Accounts")" class="icontext">
                <img src="~/Assets/images/avatars/1.jpg" class="icon avatar-sm" alt="">
                <div class="text">
                    @if (Session["userId"] != null)
                    { <h6 class="mb-0">@Session["userId"]</h6>}
                    <small>View profile</small>
                </div>
            </a>
        </header>
        <article class="offcanvas-body">
            <nav class="nav-sidebar mt-2">

                @if (Session["menuLeft"] != null)
                {
                    var leftMenu = (List<PriOrder.App.Models.WO_APP_MENU>)Session["menuLeft"];
                    foreach (var _menu in leftMenu)
                    {
                        <a href="@Url.Action(_menu.ACTION_NAME,_menu.CONTROLLER_NAME)"> <i class="material-icons @_menu.MENU_ICON"></i> @_menu.MENU_NAME_EN</a>
                    }
                }

                @*<a href="@Url.Action("Products")"> <i class="material-icons md-local_offer"></i> Products</a>
                    <a href="@Url.Action("MyOrders")"> <i class="material-icons md-local_shipping"></i> My Orders</a>
                    <a href="@Url.Action("Damages")"> <i class="material-icons md-store"></i> Damages </a>
                    <hr>
                    <a href="@Url.Action("Messages")"> <i class="material-icons md-chat"></i> Messages</a>
                    <a href="@Url.Action("Support")"> <i class="material-icons md-question_answer"></i>Help &amp; Support</a>
                    <a href="#3"> <i class="material-icons md-info"></i> About us</a>
                    <hr>*@
                <a href="@Url.Action("MyProfile","Home")"> <i class="material-icons md-account_circle"></i> Profile </a>
                <a href="@Url.Action("ChangePassword")"> <i class="material-icons md-lock"></i> Change Password</a>
                <hr>
                <a href="@Url.Action("Logout","Home")"> <i class="material-icons md-log_out"></i> Logout</a>
            </nav>
        </article> <!-- offcanvas-body .// -->
    </aside> <!-- offcanvas.// -->
    <!-- jQuery -->
    <script src="~/Assets/js/jquery-3.5.0.min.js" type="text/javascript"></script>

    <!-- Bootstrap 5 JS -->
    <script src="~/Assets/js/bootstrap.bundle.js" type="text/javascript"></script>

    <!-- custom javascript -->
    <script src="~/Assets/js/script.js" type="text/javascript"></script>

    <!-- PWA -->
    @*<script src="js/pwa.js"></script>*@


    @RenderSection("scripts", required: false)

    <script>
        $(document).ready(function () {
            var url = window.location;
            $('nav.nav-bottom a').filter(function () {
                return this.href == url;
            }).addClass('active');
            $('nav.nav-sidebar a').filter(function () {
                return this.href == url;
            }).addClass('bg-gradient-blue');
        });
    </script>
    <script>
        $('.minus').click(function () {
            quantityField = $(this).next();
            if (quantityField.val() != 1) {
                quantityField.val(parseInt(quantityField.val(), 10) - 1);
            }
        });
        $('.plus').click(function () {
            quantityField = $(this).prev();
            quantityField.val(parseInt(quantityField.val(), 10) + 1);
        });
    </script>
</body>
</html>