@model IEnumerable<PriOrder.App.Models.WO_APP_MENU>

@if (Model != null || Model.Count() > 0)
{
    foreach (var item in Model)
    {
        if (Request.Browser.IsMobileDevice)
        {
            <a href="@Url.Action(item.ACTION_NAME, item.CONTROLLER_NAME)" class="nav-link">
                <i class="icon material-icons @item.MENU_ICON"></i>
                @if (item.MENU_ID == 20010003)
                {
                    <span class='badge bg-warning'>@item.MODULE_ID</span>
                }
                <span class="text">@item.MENU_NAME_EN</span>
            </a>
        }
        else
        {
            <a href="@Url.Action(item.ACTION_NAME, item.CONTROLLER_NAME)" class="btn btn-primary btn-sm d-inline-block">
                <i class="material-icons @item.MENU_ICON"></i>
                @item.MENU_NAME_EN
                @if (item.MENU_ID == 20010003)
                {
                    <span class='badge bg-warning'>@item.MODULE_ID</span>
                }
            </a>
        }
    }
}



        @*@if (Session["menuBottom"] != null)
            {
                var bottomMenu = (List<PriOrder.App.Models.WO_APP_MENU>)Session["menuBottom"];
                foreach (var _menu in bottomMenu)
                {
                    <a href="@Url.Action(_menu.ACTION_NAME,_menu.CONTROLLER_NAME)" class="nav-link">
                        <i class="icon material-icons @_menu.MENU_ICON"></i><span class="text">@_menu.MENU_NAME_EN</span>
                    </a>
                }
            }*@
