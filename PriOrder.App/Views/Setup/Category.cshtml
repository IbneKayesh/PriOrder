@model  PriOrder.App.Models.WO_ITEM_TYPE
@{
    ViewBag.Title = "Category";
}


<section class="p-3">
    @using (Html.BeginForm("Category", "Setup", FormMethod.Post, new { enctype = "multipart/form-data" }))
    {
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.ITEM_TYPE_ID)
        <div class="mb-3">
            <div class="icontext">
                <div class="icon">
                    <img id="cat-images" src="@Model.ITEM_TYPE_IMAGE" class="avatar-lg">
                </div>
                <div class="text">
                    <label for="formFile" class="form-label">Change image</label>
                    <input class="form-control" type="file" name="ITEM_TYPE_IMAGE" onchange="viewImage(this);" accept="image/jpeg" required>
                </div>
            </div>
        </div>
        <div class="mb-3">
            <label class="form-label">@Model.ITEM_TYPE_ID</label>
            <input type="text" required="" class="form-control" value="@Model.ITEM_TYPE_NAME" readonly>
        </div>
        <button type="submit" class="btn btn-primary w-100"> Update  </button>
    }
</section>
@section scripts{
    <script>
        function viewImage(input) {
            if (input.files && input.files[0]) {
                var reader = new FileReader();
                reader.onload = function (e) {
                    $('#cat-images').attr('src', e.target.result).width(80).height(80);
                };
                reader.readAsDataURL(input.files[0]);
            }
        }
    </script>
}